# Personal Dotfiles

## Requirements

- Homebrew

## Setup

```sh
mkdir ~/.config
sh brew.sh
aqua i -a -l
sh install.sh
ya pkg install
```

### Windows(WSL)

1. Install WSL2 with Debian
2. Install fonts manually
3. Copy WezTerm config to Windows .config directory
